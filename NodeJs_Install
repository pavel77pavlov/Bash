#How to install NodeJS 16 on Ubuntu

curl -fsSL https://deb.nodesource.com/setup_16.x | sudo -E bash -
sudo apt-get install -y nodejs

#additions

sudo npm cache clean -f
sudo npm install -g n
sudo n stable
